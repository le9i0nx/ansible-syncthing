#{{ ansible_managed }}
description "Syncthing"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [!2345]
env STNORESTART=yes
setuid "{{role_syncthing_user}}"
setgid "{{role_syncthing_user}}"

exec {{syncthing_path}}/syncthing -no-browser -logflags=0

respawn
